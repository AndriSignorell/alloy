% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/binnedResid.R
\name{panelBinnedResid}
\alias{panelBinnedResid}
\title{Facet Panel for Binned Residuals}
\usage{
panelBinnedResid(x, y, lci, uci, col, pch = 16, bandCol = NULL, ...)
}
\arguments{
\item{x, y}{the bin midpoints and mean residuals, supplied by
\code{plotFacet} from the sample's \code{x} and \code{y} components.}

\item{lci, uci}{the band, supplied from the sample's components of the
same name.}

\item{col, pch}{colour and symbol of the bin means, supplied by
\code{plotFacet}.}

\item{bandCol}{fill colour of the band. \code{NULL} (default) takes it
from the active theme.}

\item{...}{further arguments passed to \code{\link{points}}.}
}
\value{
called for its side effect; returns \code{NULL} invisibly.
}
\description{
A panel function for \code{\link[pharos]{plotFacet}} that draws one
binned residual panel: the band, the zero line and the bin means. Pass
the output of \code{\link{binnedResid}} as the samples.
}
\examples{
fitLogit <- fitMod(admit ~ gre + gpa + rank, Admit, fitfn = "logit")
bins <- binnedResid(fitLogit, var = predictors(fitLogit))

pharos::plotFacet(bins, dim = c(1, 3), panelFun = panelBinnedResid,
                  xlim = lapply(bins, function(b) range(b$x)),
                  ylim = range(unlist(lapply(bins, function(b) c(b$lci, b$uci)))),
                  stripLabels = vars, ylab = "mean residual")

}
\seealso{
\link{model-diagnostics-overview} for an overview of the diagnostics for
logistic models in alloy.
}
\concept{binary-outcome}
\concept{panel}
\concept{regression-diagnostics}
