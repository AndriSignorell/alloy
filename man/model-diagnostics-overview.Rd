% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-diagnostics-overview.R
\name{model-diagnostics-overview}
\alias{model-diagnostics-overview}
\title{Diagnostics for Logistic Models in alloy}
\description{
A fitted logistic model cannot be checked with the plots that work for
a linear one. With a binary response the residuals carry no scatter to
inspect - they lie on the two curves \eqn{-\hat p} and \eqn{1-\hat p} -
so every diagnostic here either aggregates observations before looking
at them, or transforms them into something that is continuous under the
model.

\strong{Panels of \code{plot()}} — drawn by \code{\link[=plot.FitMod]{plot.FitMod()}} via \code{which}:\tabular{lll}{
   Panel \tab Function \tab Question \cr
   1 \tab \code{\link[=plotBinnedResid]{plotBinnedResid()}} \tab is the functional form right \cr
   2 \tab \code{\link[=plotCalibration]{plotCalibration()}} \tab are the predicted risks right \cr
   3 \tab \code{\link[=quantileResid]{quantileResid()}} + \code{plotQQ()} \tab is the whole distribution right \cr
   4 \tab \code{\link[=plotInfluence]{plotInfluence()}} \tab which observations drive the fit \cr
   5 \tab \code{\link[=plotSeparation]{plotSeparation()}} \tab do the predictions order the outcomes \cr
}


\strong{Called directly} — one panel per term, so no fixed panel number:\tabular{ll}{
   Function \tab Question \cr
   \code{\link[=plotPartialResid]{plotPartialResid()}} \tab is this term linear in the logit \cr
   \code{\link[=plotBinnedResid]{plotBinnedResid()}} with \code{var} \tab where in this predictor does the fit break \cr
}
}
\section{Choosing among them}{

The three that answer different questions are the calibration curve,
the binned residuals per predictor, and the influence plot: level,
shape, and individual observations. The others refine rather than add.

A ROC curve is not in this list on purpose. It measures discrimination
and is invariant to any monotone transformation of the predicted
probabilities, so it cannot detect miscalibration and barely responds
to a misspecified functional form. It answers a real question, just not
this one - see \code{\link[=roc]{roc()}} and \code{\link[DescToolsX:cStat]{cStat()}}.

The formal counterparts to these plots live in \pkg{lumen}:
\code{hosmerLemeshowTest()} for the grouped goodness-of-fit test that panel 2
shows graphically, and \code{leCessieTest()} for the smoothed-residual test.
A test returns one p-value; the plot says where the misfit is, which is
the part that tells you what to change.
}

\section{On the development sample}{

Calibration intercept and slope are 0 and 1 by construction when a
logistic model is evaluated on the data it was fitted to, so their
in-sample values say nothing about overfitting. Pass \code{newdata} to
\code{\link[=plotCalibration]{plotCalibration()}}, or resample, before reading them as evidence.
}

\concept{binary-outcome}
\concept{calibration}
\concept{regression-diagnostics}
