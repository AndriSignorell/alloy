% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coeffDiffCI.R
\name{coeffDiffCI}
\alias{coeffDiffCI}
\title{Confidence Interval for the Difference of Two Regression Coefficients}
\usage{
coeffDiffCI(
  x,
  coeff,
  conf.level = 0.95,
  sides = c("two.sided", "left", "right"),
  vcov. = vcov
)
}
\arguments{
\item{x}{a fitted model object of class \code{"lm"}.}

\item{coeff}{two coefficients, given as names or as (integer) positions
in \code{coef(x)}. The difference is taken as second minus first,
\code{coeff[2] - coeff[1]}.}

\item{conf.level}{confidence level of the interval, default \code{0.95}.}

\item{sides}{a character string specifying the side of the confidence
interval, one of \code{"two.sided"} (default), \code{"left"}, or
\code{"right"}.}

\item{vcov.}{covariance matrix of the coefficient estimates, or a
function to compute it from \code{x} (default \code{\link{vcov}}).
Supply e.g. \code{sandwich::vcovHC} for heteroskedasticity-robust
intervals.}
}
\value{
a named numeric vector with components \code{diff},
\code{lci}, and \code{uci}.
}
\description{
Computes the Wald confidence interval for the difference
\eqn{b_2 - b_1} of two coefficients of a fitted linear model, using
\eqn{Var(b_2 - b_1) = Var(b_1) + Var(b_2) - 2\,Cov(b_1, b_2)} and the
t-distribution with the model's residual degrees of freedom.
}
\examples{
fit <- lm(mpg ~ cyl + disp + hp, data = mtcars)
coeffDiffCI(fit, c("cyl", "hp"))

}
