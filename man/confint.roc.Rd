% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roc.R
\name{confint.roc}
\alias{confint.roc}
\title{Confidence intervals for ROC curve coordinates}
\usage{
\method{confint}{roc}(object, parm, level = 0.95, x = "best", ...)
}
\arguments{
\item{object}{An object of class \code{"roc"} as returned by
\code{\link[pROC]{roc}} or \code{\link{roc}}.}

\item{parm}{Currently unused; present for compatibility with the generic
\code{\link[stats]{confint}}.}

\item{level}{Confidence level.  Default is \code{0.95}.}

\item{x}{Coordinate at which to evaluate the confidence interval.
Either a numeric value (e.g. a threshold or specificity value) or
one of the special strings accepted by
\code{\link[pROC]{ci.coords}}: \code{"best"} (default),
\code{"all"}, or \code{"local maximas"}.}

\item{...}{Further arguments passed to \code{\link[pROC]{ci.coords}}.}
}
\value{
A \code{"ci.coords"} object as returned by
\code{\link[pROC]{ci.coords}}.
}
\description{
Computes confidence intervals for sensitivity, specificity, or other
coordinates of an ROC curve via \code{\link[pROC]{ci.coords}}.
}
\examples{
fitLogit <- fitMod(admit ~ gre + gpa + rank, Admit, fitfn = "logit")
r <- roc(fitLogit)
confint(r)                    # CI at best cut-point
confint(r, x = 0.5)          # CI at specificity = 0.5

}
\seealso{
\code{\link[pROC]{ci.coords}}, \code{\link{roc}}
}
